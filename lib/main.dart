import 'package:flutter/material.dart';

void main() {
  runApp(Xo());
}

class Xo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Column(children: [
          Expanded(child: Text('Amir88888888ooooooa')),
          Expanded(child: Text('ahmed')),
        ]),
      ),
    );
  }
}
